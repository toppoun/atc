[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "atc"
version = "0.1.0"
description = "AtCoder helper CLI"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
  "online-judge-tools"
]

[project.scripts]
atc = "atc.cli:main"
